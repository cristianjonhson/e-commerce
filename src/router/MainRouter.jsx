import { BrowserRouter, Routes, Route } from "react-router-dom";

const MainRouter = () => {
  return (
    <BrowserRouter>
      <Routes>
        <Route />
      </Routes>
    </BrowserRouter>
  );
};

export default MainRouter;
